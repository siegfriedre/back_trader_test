# 核心代码提交与验证范围

本文件随熊市 ROC 过渡仓和独立信号价格代码一同版本化，不再只包含工作流。
代码基线为 `7a9410295d9ef06bf962fc68915b9eb164a7cb13`；保留现有开发分支，未要求合并 main 或新建 PR。

## 本次重新执行的验证

使用 unittest 合并运行以下四个测试文件，共 **110 tests / OK**：

- `tests/test_comparison.py`：41 项。
- `tests/test_research_model.py`：31 项。
- `tests/test_review_events.py`：8 项。
- `tests/test_signal_bridge.py`：30 项。

本地环境为 pandas 2.2.3、NumPy 2.3.5、exchange-calendars 4.13.1。未安装 yfinance，因此本地没有重新运行原数据下载层的24项测试；这不是完整134项套件通过的声明。完整套件由现有 GitHub Actions 配置执行，实际状态以对应提交的运行记录为准。

测试中的行情是明确标注的人造样例，包括4月1日信号、下一交易日成交的时序样例。单日参考值用于核对公式和下载数据，不用于硬编码买卖日期。测试通过不能当作新版真实行情业绩。

## 本次提交内容

- `bear_roc_bridge`：均线下新的 ROC 下穿触发 QQQ 过渡仓，默认70%，可以通过 `--bridge-weight` 修改。已有 QQQ 抄底仓计入目标，不额外叠加70%。
- 过渡仓持续到恢复 MA200；期间 ROC 重新转正仍持有 QQQ。恢复时 ROC 为负进入所选杠杆档位，ROC 为正保持70% QQQ。
- 信号价与分红复投近似收益分离；保留旧策略，原 CSV 不变。
- 启动入口、逐笔日志、信号差异报告、截图单日核验和独立策略说明。

## 尚未验收

本次本地未进行在线信号价下载、新版真实行情回测，或完整 TradingView 历史逐日对账。下载、结构校验或单日参考核对失败时程序阻止发布回测结果，不退回旧调整价信号，也不为通过核对而篡改价格。

分红仍按原收益序列的复投近似核算，不重复增加现金，不是券商实际到账和 DRIP 股数账本。

运行命令见 `START_HERE.md`；规则及参数见 `docs/strategies/bear_roc_bridge.md`。
