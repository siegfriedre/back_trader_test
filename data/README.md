# 旧数据说明

本目录已有的 `*_synthetic_daily.csv` 含代理/模拟历史，`raw/` 来自旧下载脚本，未记录完整来源与复权参数。**请勿将其直接用于要求真实历史行情的回测，也不要重命名成新流程的数据文件。**

旧文件保留供追溯。运行仓库根目录的 `get_data.py` 重新下载，新批次保存到 `verified/`。回测通过 `load_verified_data()` 检查并读取新数据，详见根目录 README。
